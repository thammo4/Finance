import config;
import requests;
import pandas as pd;


# import requests
# import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns



sym = 'ETHBUSD';
r = requests.get('{}/api/v3/depth'.format(config.API_BASE_URL), params={'symbol':sym});
results = r.json();
# frames = {side: pd.DataFrame(data=results[side], columns=["price", "quantity"],
#                              dtype=float)
#           for side in ["bids", "asks"]}
frames = {side: pd.DataFrame(data=results[side], columns={'price', 'quantity'}, dtype=float) for side in ['bids', 'asks']};

# concatenate sides into singe data frame
frames_list = [frames[side].assign(side=side) for side in frames];
data = pd.concat(frames_list, axis='index', ignore_index=True, sort =True);
print(sym);
print(data); print('\n');

price_summary = data.groupby("side").price.describe()
price_summary.to_markdown()

print(price_summary); print('\n');

print('max = ' + str(frames['bids'].price.max())); print('\n');
print('min = ' + str(frames['bids'].price.min())); print('\n');


url = 






































































































































































































































































































































































































































































































































































